---
# Rocky Linux-specific variables
package_manager: dnf
update_cache: yes
cache_valid_time: 3600

# Common packages for Rocky
common_packages:
  - curl
  - wget
  - vim
  - htop
  - git
  - yum-cron

# Service names mapping  
service_names:
  ssh: sshd
  ntp: chronyd
  cron: crond
